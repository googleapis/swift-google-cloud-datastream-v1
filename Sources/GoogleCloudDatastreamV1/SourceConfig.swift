// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) public import GoogleWKT

/// The configuration of the stream source.
public struct SourceConfig: Codable, Equatable, GoogleWKT._AnyPackable,
  Sendable
{
  /// Required. Source connection profile resource.
  /// Format: `projects/{project}/locations/{location}/connectionProfiles/{name}`
  public var sourceConnectionProfile: Swift.String = Swift.String()

  /// Stream configuration that is specific to the data source type.
  public var sourceStreamConfig: OneOf_SourceStreamConfig? = nil

  @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

  /// Initialize a new instance of `SourceConfig`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = SourceConfig().with { $0.sourceConnectionProfile = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private struct CodingKeys: CodingKey {
    var stringValue: Swift.String
    var intValue: Swift.Int? { nil }
    init(stringValue: Swift.String) { self.stringValue = stringValue }
    init?(intValue: Swift.Int) { nil }

    static let sourceConnectionProfile = CodingKeys(stringValue: "sourceConnectionProfile")
    static let oracleSourceConfig = CodingKeys(stringValue: "oracleSourceConfig")
    static let mysqlSourceConfig = CodingKeys(stringValue: "mysqlSourceConfig")
    static let postgresqlSourceConfig = CodingKeys(stringValue: "postgresqlSourceConfig")
    static let sqlServerSourceConfig = CodingKeys(stringValue: "sqlServerSourceConfig")
    static let salesforceSourceConfig = CodingKeys(stringValue: "salesforceSourceConfig")
    static let mongodbSourceConfig = CodingKeys(stringValue: "mongodbSourceConfig")

    static let _knownKeys: Set<Swift.String> = [
      "sourceConnectionProfile",
      "oracleSourceConfig",
      "mysqlSourceConfig",
      "postgresqlSourceConfig",
      "sqlServerSourceConfig",
      "salesforceSourceConfig",
      "mongodbSourceConfig",
    ]
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    if let value = try container.decodeIfPresent(
      Swift.String.self, forKey: .sourceConnectionProfile)
    {
      self.sourceConnectionProfile = value
    }

    var sourceStreamConfig: OneOf_SourceStreamConfig? = nil
    let sourceStreamConfigCheckAndSet = {
      if sourceStreamConfig != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'sourceStreamConfig'"))
      }
      sourceStreamConfig = $0
    }
    if let oracleSourceConfig = try container.decodeIfPresent(
      OracleSourceConfig?.self, forKey: .oracleSourceConfig)
    {
      try sourceStreamConfigCheckAndSet(.oracleSourceConfig(oracleSourceConfig))
    }
    if let mysqlSourceConfig = try container.decodeIfPresent(
      MysqlSourceConfig?.self, forKey: .mysqlSourceConfig)
    {
      try sourceStreamConfigCheckAndSet(.mysqlSourceConfig(mysqlSourceConfig))
    }
    if let postgresqlSourceConfig = try container.decodeIfPresent(
      PostgresqlSourceConfig?.self, forKey: .postgresqlSourceConfig)
    {
      try sourceStreamConfigCheckAndSet(.postgresqlSourceConfig(postgresqlSourceConfig))
    }
    if let sqlServerSourceConfig = try container.decodeIfPresent(
      SqlServerSourceConfig?.self, forKey: .sqlServerSourceConfig)
    {
      try sourceStreamConfigCheckAndSet(.sqlServerSourceConfig(sqlServerSourceConfig))
    }
    if let salesforceSourceConfig = try container.decodeIfPresent(
      SalesforceSourceConfig?.self, forKey: .salesforceSourceConfig)
    {
      try sourceStreamConfigCheckAndSet(.salesforceSourceConfig(salesforceSourceConfig))
    }
    if let mongodbSourceConfig = try container.decodeIfPresent(
      MongodbSourceConfig?.self, forKey: .mongodbSourceConfig)
    {
      try sourceStreamConfigCheckAndSet(.mongodbSourceConfig(mongodbSourceConfig))
    }
    self.sourceStreamConfig = sourceStreamConfig
    for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
      self._unknownFields.json[key.stringValue] = try container.decode(
        GoogleWKT.Value.self, forKey: key)
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.sourceConnectionProfile, forKey: .sourceConnectionProfile)

    if let choice = self.sourceStreamConfig {
      switch choice {
      case .oracleSourceConfig(let value):
        try container.encode(value, forKey: .oracleSourceConfig)
      case .mysqlSourceConfig(let value):
        try container.encode(value, forKey: .mysqlSourceConfig)
      case .postgresqlSourceConfig(let value):
        try container.encode(value, forKey: .postgresqlSourceConfig)
      case .sqlServerSourceConfig(let value):
        try container.encode(value, forKey: .sqlServerSourceConfig)
      case .salesforceSourceConfig(let value):
        try container.encode(value, forKey: .salesforceSourceConfig)
      case .mongodbSourceConfig(let value):
        try container.encode(value, forKey: .mongodbSourceConfig)
      }
    }
    for (key, value) in self._unknownFields.json {
      try container.encode(value, forKey: CodingKeys(stringValue: key))
    }
  }

  /// Stream configuration that is specific to the data source type.
  public enum OneOf_SourceStreamConfig: Codable, Equatable, Sendable {
    /// Oracle data source configuration.
    indirect case oracleSourceConfig(OracleSourceConfig?)
    /// MySQL data source configuration.
    indirect case mysqlSourceConfig(MysqlSourceConfig?)
    /// PostgreSQL data source configuration.
    indirect case postgresqlSourceConfig(PostgresqlSourceConfig?)
    /// SQLServer data source configuration.
    indirect case sqlServerSourceConfig(SqlServerSourceConfig?)
    /// Salesforce data source configuration.
    indirect case salesforceSourceConfig(SalesforceSourceConfig?)
    /// MongoDB data source configuration.
    indirect case mongodbSourceConfig(MongodbSourceConfig?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.datastream.v1.SourceConfig"
  }
  public init(fromAny any: GoogleWKT.`Any`) throws {
    self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleWKT.Struct {
    return try GoogleWKT._slowAnySerialize(message: self)
  }
}
