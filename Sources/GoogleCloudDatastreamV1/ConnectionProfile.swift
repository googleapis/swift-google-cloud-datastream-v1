// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWkt

/// A set of reusable connection configurations to be used as a source or
/// destination for a stream.
public struct ConnectionProfile: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Output only. Identifier. The resource's name.
  public var name: Swift.String = Swift.String()

  /// Output only. The create time of the resource.
  public var createTime: GoogleCloudWkt.Timestamp? = nil

  /// Output only. The update time of the resource.
  public var updateTime: GoogleCloudWkt.Timestamp? = nil

  /// Labels.
  public var labels: [Swift.String: Swift.String] = [:]

  /// Required. Display name.
  public var displayName: Swift.String = Swift.String()

  /// Output only. Reserved for future use.
  public var satisfiesPzs: Swift.Bool? = nil

  /// Output only. Reserved for future use.
  public var satisfiesPzi: Swift.Bool? = nil

  /// Connection configuration for the ConnectionProfile.
  public var profile: OneOf_Profile? = nil

  /// Connectivity options used to establish a connection to the profile.
  public var connectivity: OneOf_Connectivity? = nil

  /// Initialize a new instance of `ConnectionProfile`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = ConnectionProfile().with { $0.name = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case name = "name"
    case createTime = "createTime"
    case updateTime = "updateTime"
    case labels = "labels"
    case displayName = "displayName"
    case satisfiesPzs = "satisfiesPzs"
    case satisfiesPzi = "satisfiesPzi"
    case oracleProfile = "oracleProfile"
    case gcsProfile = "gcsProfile"
    case mysqlProfile = "mysqlProfile"
    case bigqueryProfile = "bigqueryProfile"
    case postgresqlProfile = "postgresqlProfile"
    case sqlServerProfile = "sqlServerProfile"
    case salesforceProfile = "salesforceProfile"
    case mongodbProfile = "mongodbProfile"
    case staticServiceIpConnectivity = "staticServiceIpConnectivity"
    case forwardSshConnectivity = "forwardSshConnectivity"
    case privateConnectivity = "privateConnectivity"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.name = try container.decode(Swift.String.self, forKey: .name)
    self.createTime = try container.decodeIfPresent(
      GoogleCloudWkt.Timestamp.self, forKey: .createTime)
    self.updateTime = try container.decodeIfPresent(
      GoogleCloudWkt.Timestamp.self, forKey: .updateTime)
    self.labels = try container.decode([Swift.String: Swift.String].self, forKey: .labels)
    self.displayName = try container.decode(Swift.String.self, forKey: .displayName)
    self.satisfiesPzs = try container.decodeIfPresent(Swift.Bool.self, forKey: .satisfiesPzs)
    self.satisfiesPzi = try container.decodeIfPresent(Swift.Bool.self, forKey: .satisfiesPzi)

    var profile: OneOf_Profile? = nil
    let profileCheckAndSet = {
      if profile != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'profile'"))
      }
      profile = $0
    }
    if let oracleProfile = try container.decodeIfPresent(
      OracleProfile?.self, forKey: .oracleProfile)
    {
      try profileCheckAndSet(.oracleProfile(oracleProfile))
    }
    if let gcsProfile = try container.decodeIfPresent(GcsProfile?.self, forKey: .gcsProfile) {
      try profileCheckAndSet(.gcsProfile(gcsProfile))
    }
    if let mysqlProfile = try container.decodeIfPresent(MysqlProfile?.self, forKey: .mysqlProfile) {
      try profileCheckAndSet(.mysqlProfile(mysqlProfile))
    }
    if let bigqueryProfile = try container.decodeIfPresent(
      BigQueryProfile?.self, forKey: .bigqueryProfile)
    {
      try profileCheckAndSet(.bigqueryProfile(bigqueryProfile))
    }
    if let postgresqlProfile = try container.decodeIfPresent(
      PostgresqlProfile?.self, forKey: .postgresqlProfile)
    {
      try profileCheckAndSet(.postgresqlProfile(postgresqlProfile))
    }
    if let sqlServerProfile = try container.decodeIfPresent(
      SqlServerProfile?.self, forKey: .sqlServerProfile)
    {
      try profileCheckAndSet(.sqlServerProfile(sqlServerProfile))
    }
    if let salesforceProfile = try container.decodeIfPresent(
      SalesforceProfile?.self, forKey: .salesforceProfile)
    {
      try profileCheckAndSet(.salesforceProfile(salesforceProfile))
    }
    if let mongodbProfile = try container.decodeIfPresent(
      MongodbProfile?.self, forKey: .mongodbProfile)
    {
      try profileCheckAndSet(.mongodbProfile(mongodbProfile))
    }
    self.profile = profile

    var connectivity: OneOf_Connectivity? = nil
    let connectivityCheckAndSet = {
      if connectivity != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'connectivity'"))
      }
      connectivity = $0
    }
    if let staticServiceIpConnectivity = try container.decodeIfPresent(
      StaticServiceIpConnectivity?.self, forKey: .staticServiceIpConnectivity)
    {
      try connectivityCheckAndSet(.staticServiceIpConnectivity(staticServiceIpConnectivity))
    }
    if let forwardSshConnectivity = try container.decodeIfPresent(
      ForwardSshTunnelConnectivity?.self, forKey: .forwardSshConnectivity)
    {
      try connectivityCheckAndSet(.forwardSshConnectivity(forwardSshConnectivity))
    }
    if let privateConnectivity = try container.decodeIfPresent(
      PrivateConnectivity?.self, forKey: .privateConnectivity)
    {
      try connectivityCheckAndSet(.privateConnectivity(privateConnectivity))
    }
    self.connectivity = connectivity
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.createTime, forKey: .createTime)
    try container.encode(self.updateTime, forKey: .updateTime)
    try container.encode(self.labels, forKey: .labels)
    try container.encode(self.displayName, forKey: .displayName)
    try container.encode(self.satisfiesPzs, forKey: .satisfiesPzs)
    try container.encode(self.satisfiesPzi, forKey: .satisfiesPzi)

    if let choice = self.profile {
      switch choice {
      case .oracleProfile(let value):
        try container.encode(value, forKey: .oracleProfile)
      case .gcsProfile(let value):
        try container.encode(value, forKey: .gcsProfile)
      case .mysqlProfile(let value):
        try container.encode(value, forKey: .mysqlProfile)
      case .bigqueryProfile(let value):
        try container.encode(value, forKey: .bigqueryProfile)
      case .postgresqlProfile(let value):
        try container.encode(value, forKey: .postgresqlProfile)
      case .sqlServerProfile(let value):
        try container.encode(value, forKey: .sqlServerProfile)
      case .salesforceProfile(let value):
        try container.encode(value, forKey: .salesforceProfile)
      case .mongodbProfile(let value):
        try container.encode(value, forKey: .mongodbProfile)
      }
    }

    if let choice = self.connectivity {
      switch choice {
      case .staticServiceIpConnectivity(let value):
        try container.encode(value, forKey: .staticServiceIpConnectivity)
      case .forwardSshConnectivity(let value):
        try container.encode(value, forKey: .forwardSshConnectivity)
      case .privateConnectivity(let value):
        try container.encode(value, forKey: .privateConnectivity)
      }
    }
  }

  /// Connection configuration for the ConnectionProfile.
  public enum OneOf_Profile: Codable, Equatable, Sendable {
    /// Oracle ConnectionProfile configuration.
    indirect case oracleProfile(OracleProfile?)
    /// Cloud Storage ConnectionProfile configuration.
    indirect case gcsProfile(GcsProfile?)
    /// MySQL ConnectionProfile configuration.
    indirect case mysqlProfile(MysqlProfile?)
    /// BigQuery Connection Profile configuration.
    indirect case bigqueryProfile(BigQueryProfile?)
    /// PostgreSQL Connection Profile configuration.
    indirect case postgresqlProfile(PostgresqlProfile?)
    /// SQLServer Connection Profile configuration.
    indirect case sqlServerProfile(SqlServerProfile?)
    /// Salesforce Connection Profile configuration.
    indirect case salesforceProfile(SalesforceProfile?)
    /// MongoDB Connection Profile configuration.
    indirect case mongodbProfile(MongodbProfile?)
  }

  /// Connectivity options used to establish a connection to the profile.
  public enum OneOf_Connectivity: Codable, Equatable, Sendable {
    /// Static Service IP connectivity.
    indirect case staticServiceIpConnectivity(StaticServiceIpConnectivity?)
    /// Forward SSH tunnel connectivity.
    indirect case forwardSshConnectivity(ForwardSshTunnelConnectivity?)
    /// Private connectivity.
    indirect case privateConnectivity(PrivateConnectivity?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.datastream.v1.ConnectionProfile"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
